Place a demo video at:

- `backend/data/sample.mp4`

The backend uses this file when a camera stream URL is set to `sim://sample`.
If no file exists, the backend generates synthetic frames so the app still works.
